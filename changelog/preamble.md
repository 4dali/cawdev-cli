# Changelog

> **Generated. Do not edit.**
>
> This file is an export of cawdev's own `changelog_entry` table. A hand edit
> here is lost at the next export. Add an entry in the console, or through the
> MCP server, then regenerate:
>
> ```sh
> node tools/changelog/export.mjs cawdev
> ```

All notable changes to cawdev are recorded here. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and the project
follows [semantic versioning](https://semver.org/spec/v2.0.0.html).

Releases are newest first, with `Unreleased` above them. **Every release gets
changelog entries and a roadmap pass** — a fix without a roadmap entry is
normal; a shipped feature without one is not.
